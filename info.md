# Python Flask CI/CD Lab

This is a Python Flask application designed for a CI/CD lab. The project demonstrates how to set up continuous integration and continuous deployment workflows using GitHub Actions and GitHub Copilot.

## Installing Requirements

To install the required dependencies, run:

```bash
pip install -r requirements.txt
```

## Running the Tests

To run the tests, use:

```bash
python -m pytest
```
